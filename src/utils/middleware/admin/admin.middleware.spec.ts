import { AdminMiddleware } from './admin.middleware';

describe('AdminMiddleware', () => {
  let service: AdminMiddleware;
  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});
