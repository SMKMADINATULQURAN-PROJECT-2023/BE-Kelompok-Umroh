import { TrafficMiddleware } from './traffic.middleware';

describe('TrafficMiddleware', () => {
  let service: TrafficMiddleware;
  it('should be defined', () => {
    expect(service).toBeDefined();
  });
});
